# SARTriage — Python Dependencies
# ================================
# Pinned minimum versions for reproducibility.
# Tested on Python 3.11+ (Docker) and 3.13 (development).

# ── Core ML ──
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.0.0
sahi>=0.11.0

# ── Computer Vision ──
opencv-python-headless>=4.8.0

# ── ML Utilities ──
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.11.0

# ── Web Framework ──
flask>=3.0.0
gunicorn>=21.2.0

# ── Configuration & Serialisation ──
pyyaml>=6.0

# ── Plotting (evaluation only, not required at runtime) ──
matplotlib>=3.7.0
