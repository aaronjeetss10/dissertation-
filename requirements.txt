# SARTriage — Python Dependencies
# ================================
# Pinned versions for reproducibility (audited 2026-03-26).
# Tested on Python 3.11+ (Docker) and 3.13 (development).

# ── Core ML ──
torch==2.10.0
torchvision==0.25.0
ultralytics==8.4.23
sahi==0.11.36

# ── Computer Vision ──
opencv-python-headless==4.10.0

# ── ML Utilities ──
scikit-learn==1.8.0
numpy==2.4.3
scipy==1.17.1
imbalanced-learn==0.14.1
supervision==0.27.0.post2

# ── Web Framework ──
flask==3.1.3
gunicorn>=21.2.0

# ── Configuration & Serialisation ──
pyyaml==6.0.3

# ── Plotting (evaluation only, not required at runtime) ──
matplotlib==3.10.8

# ── Testing & Linting (development only) ──
pytest>=9.0.0
flake8>=7.0.0
